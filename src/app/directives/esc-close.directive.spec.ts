import { EscCloseDirective } from './esc-close.directive';

describe('EscCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new EscCloseDirective();
    expect(directive).toBeTruthy();
  });
});
