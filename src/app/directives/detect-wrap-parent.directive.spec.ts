import { DetectWrapParentDirective } from './detect-wrap-parent.directive';

describe('DetectWrapParentDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectWrapParentDirective();
    expect(directive).toBeTruthy();
  });
});
